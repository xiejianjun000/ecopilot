# EcoPilot 项目深度诊断报告（完整版）

> 诊断日期：2026-07-19
> 诊断方法：前后端全部代码逐文件深读（非文档采信）
> 深读范围：后端 12 个 Python 文件（含 chat_api.py 5184 行全文）+ 前端 ~70 个 TypeScript/TSX 文件 + 部署配置 + git 历史
> 诊断团队：郝交付（交付总监）+ 3 路 Explore Agent 并行深读

---

## 一、项目画像

| 维度 | 现状 |
|------|------|
| 产品定位 | 面向工业企业（钢铁/水泥/火电）的生态环境合规 AI 管家 |
| 技术栈 | Next.js 16 + React 19 + Tailwind 4 ｜ FastAPI + DeepSeek + Kimi ｜ Playwright 自动化 ｜ Hermes 4 层 AI 架构 |
| AI 基座 | hermes-agent（4 层架构理念来源）→ hermes_adapter.py（落地实现，2.5 层） |
| 仓库结构 | `desktop/`（主应用 pnpm monorepo）、`ecopilot-website/`（官网+认证+支付）、`hermes-agent/`（AI 框架基座）、`dev-archive/`（归档） |
| 部署架构 | Docker Compose 6 微服务（website_api/auth/subscription/payment/hermes_admin/chat_api）+ Nginx 反代 |
| 工程化 | CI（tsc+oxlint+vitest+pytest）、AI 自愈 CI、监控 SDK、设计 token 系统 |

**一句话定性**：产品定位清晰、垂直场景扎实、Hermes AI 架构理念有前瞻性，但工程实现存在严重安全泄露（P0 级）、授权体系后门、上帝文件和大量静默失败——"快速原型的骨架已搭好，但关键关节还没焊牢"。

---

## 二、P0 问题 — 立刻处理（安全与阻塞）

### P0-1. 用户数据库泄露到 git 历史（最严重）

**证据**：
```bash
$ git ls-files ecopilot-website/deploy/data/
ecopilot-website/deploy/data/.jwt_secret        # 64字符 HMAC 密钥
ecopilot-website/deploy/data/users.json         # 用户数据（含 bcrypt 密码哈希）
ecopilot-website/deploy/data/reset_codes.json   # 明文密码重置码
ecopilot-website/deploy/data/login_attempts.json # 登录记录（IP + 手机号/邮箱）
ecopilot-website/deploy/data/invoices.json      # 发票数据
ecopilot-website/deploy/data/subscriptions.json # 订阅数据
ecopilot-website/deploy/data/usage_daily.json   # 每日用量
```

提交 commit：`819822f`（"feat: 直连大模型官方API + PDF分析Moonshot file-extract + 测试体系 + CI/CD"）

**根因**：`.gitignore` 第 42-46 行只忽略了 `.env*`，完全没有覆盖 `data/` 目录。`auth_service.py` 把 `data/` 当默认存储路径，Docker Compose 把 `./data:/app/data` 挂载到全部 6 个服务。

**泄露后果**：
- `.jwt_secret` 泄露 → 攻击者可伪造任意用户的 JWT Token，无需密码即可登录
- `users.json` 泄露 → bcrypt 哈希可离线爆破（虽然 rounds=12 增加了难度）
- `reset_codes.json` 泄露 → 明文重置码在 15 分钟有效期内可直接使用
- `login_attempts.json` 泄露 → 手机号、邮箱、IP 地址等个人信息
- 这是《个人信息保护法》和《数据安全法》级别的事故

**修复方案**：
1. 立即轮换 JWT 密钥（生成新的 `.jwt_secret`）
2. `git filter-repo` 清除 git 历史中的 `data/` 目录
3. `.gitignore` 增加 `ecopilot-website/deploy/data/` 规则
4. 已注册用户强制改密码
5. Docker Compose 按服务隔离数据卷（不要共享 `./data`）

---

### P0-2. license_manager.py 环境变量后门（授权体系形同虚设）

**证据**（`desktop/server/license_manager.py`）：
```python
# line 27-29
_secret_env = os.environ.get("ECOPILOT_LICENSE_SECRET")
if _secret_env:
    return _secret_env.encode("utf-8")
```

攻击者只需 `export ECOPILOT_LICENSE_SECRET=xxx` 然后用相同密钥签发任意授权码即可绕过机器绑定。

**额外问题**：
- `DEBUG_PROCS`（line 59）反调试名单定义了但**从未使用**——攻击者可随意 attach 调试器 patch `validate_license` 返回 True
- `_check_time`（line 159-160）检测到时间回拨后 `pass  # 记录但不阻断`——改系统时间即可延长有效期
- `get_machine_fingerprint` 在虚拟机/容器环境 uuid.getnode() 返回随机值，指纹不稳定
- 密钥存 `~/.ecopilot-home/.license_secret`，任何同权限进程可读，可自行签发授权码
- 6 处 `except: pass` 静默失败

**修复方案**：改为非对称签名（管理员私钥签发，客户端公钥验证）；或至少移除环境变量覆盖，改为编译时注入密钥。

---

### P0-3. 根 package.json 腾讯 QClaw 残留（项目身份混乱）

**证据**（`/Users/mac/Desktop/ecopilot/package.json`）：
- `productName: "QClaw"`
- `author: "Tencent"`
- 依赖 `@guanjia-openclaw/electron`、`@tencent/*` 系列
- `name: "openclaw-desktop"`

这是从腾讯 QClaw 项目 fork 后未清理的残留。影响构建配置、Electron 打包、npm 发布。

**修复方案**：清理为 EcoPilot 自己的 name/author/productName，移除 @guanjia-openclaw 和 @tencent 依赖。

---

### P0-4. 前端监控面板认证 Bug

**证据**（`desktop/frontend/app/monitor/page.tsx` line 117）：
```typescript
const token = ensureAuthToken(); // Bug: ensureAuthToken() 返回 Promise<void>
// token 永远是 undefined，后续鉴权请求实际无 token
```

正确写法应为 `await ensureAuthToken(); const token = getAuthToken();`

监控面板的 `/api/monitor/stats` 和 `/api/monitor/events` 请求实际不带认证，后端如果做了鉴权则请求全部 401，如果没做鉴权则数据裸奔。

---

## 三、P1 问题 — 尽快修复（架构与可靠性）

### P1-1. chat_api.py 5184 行上帝文件

64 路由 122 函数堆在一个文件里，涵盖 SSE 对话/工具调用/许可证/台账/档案/通知/运维/短信验证/微信扫码全模块。

**具体问题**：
- `_run` 和 `_run_vision` 的工具调用循环约 60 行几乎完全重复
- `_sessions` 字典无长度限制、无过期清理——长期运行内存泄漏
- `process_with_hermes()`（line 3648）是同步函数但在 async `_run` 中直接调用，阻塞事件循环
- `_restart_omniroute()` 在异常处理里用同步 `subprocess.run` 阻塞事件循环
- 每次对话后异步调 AI 提取"合规记忆"——成本翻倍，`compliance-memory.json` 无索引、无去重、无检索能力（最多 500 条全量加载）
- 微信扫码会话存内存（进程级），重启失效

**修复方案**：按模块拆分为 chat_router / permit_router / vault_router / notify_router / ops_router / sms_router 等，每个文件 < 500 行。

---

### P1-2. 5 个前端视图组件超千行

| 文件 | 行数 | 核心问题 |
|------|------|----------|
| views/vault.tsx | 1655 | 7 个组件挤一个文件，3 处直接 fetch 未走 lib/api |
| views/knowledge.tsx | 1207 | 25 个 useState，搜索/加载未 cancel 旧请求（竞态） |
| views/calendar.tsx | 1165 | 事件映射逻辑重复三处，generateSchedule N+1 请求 |
| views/inspection.tsx | 1120 | OCR 上传用原生 fetch 绕过 apiPost，丢失鉴权/监控 |
| views/notify.tsx | 1105 | PLATFORM_ICON/COLOR 硬编码 |

**修复方案**：每个千行文件按子组件拆分为独立文件，每个文件 < 400 行。

---

### P1-3. Hermes 基座只落地 2.5 层

Hermes 4 层架构理念（记忆/自学习/多Agent/GEPA 进化），`hermes_adapter.py`（277 行）实际实现：

| 层 | 理念 | 实现 | 缺口 |
|----|------|------|------|
| 记忆层 | 4 层记忆 | HermesMemoryLayer | json 文件存储（sessions.json/enterprises.json/regulations_cache.json），无索引、无检索 |
| 自学习层 | 自适应 | HermesSelfLearningLayer | identify_patterns 用 query[:20] 粗糙匹配，无语义理解 |
| 多Agent层 | 协作 | HermesAgentRouter | 6 个 Agent 纯关键词匹配路由，无 LLM 调度 |
| GEPA 进化层 | 进化 | **注释写了但代码未实现** | 完全缺失 |

**修复方案**：记忆层迁移 SQLite + 向量检索；Agent 路由改用 LLM function calling；GEPA 进化层至少实现 feedback→pattern→strategy 的闭环。

---

### P1-4. 前端组件层零测试

- `desktop/frontend/` 仅 3 个 lib 测试（utils/api/export），组件层和 views 层零覆盖
- `stress_test.py` 无 assert，只打印结果，CI 不友好
- 后端仅 `test_permit_parser.py` 和 `test_license_manager.py` 两个单元测试

**修复方案**：前端补 Vitest 组件测试（优先 vault/knowledge/calendar 三个千行组件）；后端补 chat_api 路由测试。

---

### P1-5. permit_scraper 硬编码 + 会话泄露

**硬编码**（4 处）：
- `username = "yuanbin"`（line 324、381、456）
- `enterpriseId = "2d3ee2db-0e80-4ec4-a3d7-322aeafc580e"`（line 324）
- `city_code = "431300000000"`（line 560）
- 碳排放报送 IP `http://114.251.10.30/#/login`（line 39）

**会话泄露**：
- `_active_sessions`（line 63）全局 dict 无锁保护，多请求并发读写有竞态
- `cleanup_stale_sessions` 需外部调用，代码中无定时调度（未启动 asyncio task 周期清理），max_age 600s 形同虚设
- `_cleanup_browser`（line 1122-1134）四个 bare `except Exception: pass` + `except: pass`，清理失败无感知，chromium 子进程泄露

**修复方案**：username/enterpriseId 从 session 配置读取；启动 asyncio task 每 5 分钟调 cleanup_stale_sessions；bare except 改为日志记录。

---

### P1-6. 大量 except: pass 静默失败

| 文件 | except: pass 数量 | 典型位置 |
|------|-------------------|----------|
| permit_scraper.py | 5 | line 355, 1096, 1122-1134 |
| license_manager.py | 6 | line 36, 84, 99, 127, 161 |
| execution_audit.py | 3 | line 180-181, 241-242 |
| tools.py | 4 | line 166, 210, 266, 332 |
| chat_api.py | 多处 | 异常处理 + _restart_omniroute |

问题被全面掩盖，生产环境出问题无从排查。

---

### P1-7. CORS 完全开放

所有 FastAPI 服务 `allow_origins=["*"]`：
- `chat_api.py`
- `auth_service.py`（line 72-76）
- `website_api.py`（line 28-33）

完全抵消了 `nginx.conf` 里精心配置的 CSP/CORS 防护。nginx 做了 HSTS/CSP/X-Frame-Options/X-Content-Type-Options 等全套安全头，但 API 服务层的 CORS `*` 让这些防护形同虚设。

**修复方案**：生产环境 CORS 改为 `allow_origins=["https://your-domain.com"]`。

---

### P1-8. auth_service.py 开发环境返回明文重置码

**证据**（`auth_service.py` line 451-455）：
```python
is_dev = os.environ.get("ECOPILOT_ENV", "development") == "development"
if is_dev:
    resp["code"] = code  # 开发环境返回重置码便于测试
```

默认值是 `"development"`——生产环境如果忘记设 `ECOPILOT_ENV=production`，任何人可以用邮箱触发忘记密码流程并直接拿到明文重置码，进而重置任意账户密码。

---

### P1-9. tasks.tsx 是 mock 未接后端

- `runNow`（line 189-196）：setTimeout 2 秒后 setToast，**未调后端**
- "全部同步"按钮（line 248-255）：仅 setToast 未实现
- 31 项 TASKS 常量全部硬编码在组件内
- 整个组件**无任何 API 调用**（纯前端）

用户看到的"执行"是假的。

---

### P1-10. knowledge_api 非原子写 + 无并发锁

- `knowledge_save`（line 420）`md_path.write_text(new_content)` 直接覆盖原文件，写入过程中崩溃会损坏文件
- `_get_vault`（line 133-138）无锁，多请求并发可能同时触发 `_scan_vault`（全盘 IO），缓存失效风暴
- `knowledge_search`（line 305-364）对每个文档 read_text 全文 + 逐行扫描，O(N*M*L)，大知识库（>1000 文档）极慢

**修复方案**：写临时文件 + rename 保证原子性；加 asyncio.Lock 防并发；搜索改用 SQLite FTS5。

---

### P1-11. ops_monitor 无数据归档

- `events` 表无 TTL/清理机制，长期运行无限增长，SQLite 单表性能下降
- `_init_db()` 模块级副作用——导入即执行建表，测试不友好
- 告警无去重，同一错误连续报错会刷屏

---

### P1-12. API 层无重试/无超时/无降级

**证据**（`desktop/frontend/lib/api.ts`）：
- 全局无错误重试机制
- 非 SSE 请求也无 timeout
- `ensureAuthToken()` 失败不重试，后续请求全 401
- `_authToken` 内存变量无并发保护（多请求同时首次调用会重复拉取 token）
- `streamSSE` 无 `id`/`event` 字段解析（仅 data）

**修复方案**：加 retry（最多 3 次，指数退避）；加 timeout（非 SSE 请求 30s）；ensureAuthToken 加 Promise 缓存防并发。

---

### P1-13. Docker Compose 所有服务共享 data 卷

**证据**（`docker-compose.yml`）：
```yaml
volumes:
  - ./data:/app/data  # 6 个服务全部挂载同一个 data 目录
```

任何一个服务被攻破，攻击者就能读写所有用户数据（密码哈希、重置码、订阅、发票）。

---

### P1-14. feedback_reader.py 不存在但 README 引用

README.md 写"通过 desktop/server/feedback_reader.py 提交反馈"，但该文件在代码库中不存在。Glob `*feedback*` 无结果，Grep `feedback_reader` 全项目仅 README.md 命中。文档与代码不一致。

---

## 四、P2 问题 — 改进级（精选）

| 编号 | 问题 | 位置 |
|------|------|------|
| P2-1 | 机器绑定授权与代码公开不匹配——代码在 GitHub 公开，授权逻辑可被分析绕过 | license_manager.py |
| P2-2 | AI 自愈 CI 每天 5 次 cron，时机不当（可能在开发高峰期触发） | .github/workflows |
| P2-3 | 说明书过度营销（"超越通用 AI"等表述），与实现差距大 | PROJECT_SPECIFICATION.md |
| P2-4 | 后端 requirements.txt 仅 7 个依赖，可能不全（bcrypt/jwt 未列出） | desktop/server/requirements.txt |
| P2-5 | execution_audit 硬编码年份 [2026, 2025, 2024]，跨年需手改代码 | execution_audit.py line 136 |
| P2-6 | 前端 3 处直接 fetch 未走 lib/api（vault 2 处 + inspection 1 处） | 见 P1-2 |
| P2-7 | calendar.tsx 事件映射逻辑重复三处 | calendar.tsx line 197-213/248-266/341-358 |
| P2-8 | doc-editor.tsx 嵌入/全屏两套渲染逻辑几乎完全重复 | doc-editor.tsx |
| P2-9 | knowledge-graph.tsx O(n²) 斥力计算 + SVG 节点完全不可键盘访问（无障碍最差） | knowledge-graph.tsx |
| P2-10 | chat-message.tsx API 地址写死 `http://127.0.0.1:8002` | chat-message.tsx line 101 |
| P2-11 | left-sidebar.tsx 企业名"冷水江钢铁"硬编码 | left-sidebar.tsx line 330 |
| P2-12 | user-panel.tsx 4 个 Tab 大量 mock 数据 | user-panel.tsx |
| P2-13 | qr-connect.tsx QR 码未实际生成（占位符） | qr-connect.tsx |
| P2-14 | global-search.tsx 法规仅 2 条占位 | global-search.tsx |
| P2-15 | exportDocx 产出 .doc 非 .docx（HTML MIME hack） | lib/export.ts |
| P2-16 | OKLCH 色彩空间无旧浏览器 fallback（Safari < 15.4） | globals.css |
| P2-17 | chat-main.tsx model 写死 "deepseek-chat" 未与设置联动 | chat-main.tsx line ~180 |
| P2-18 | tools.py _knowledge_search 与 knowledge_api.py 功能重复 | tools.py line 313 |
| P2-19 | ecopilot_mcp_server 工具数（7）与 tools.py（9）不一致 | ecopilot_mcp_server.py |
| P2-20 | code-block.tsx 暗色固定 `bg-zinc-950`，亮色主题下视觉突兀 | ui/code-block.tsx |

---

## 五、亮点（值得肯定的部分）

| 维度 | 亮点 |
|------|------|
| **产品定位** | 垂直场景扎实——排污许可/执行报告/台账/监测/整改全链条覆盖，不是"套壳 ChatGPT" |
| **AI 架构理念** | Hermes 4 层架构（记忆/自学习/多Agent/GEPA 进化）理念有前瞻性，hermes_adapter.py 已落地 2.5 层 |
| **设计系统** | Tailwind v4 + OKLCH 色阶 + 语义 token，eco 色阶 50-900 完整定义；modal.tsx 焦点陷阱+焦点恢复是最佳实践 |
| **SSE 流式** | streamSSE async generator 封装规范，doc-editor 的 AbortController 中断实现正确 |
| **状态管理** | Context + useReducer + HYDRATE 模式，SSR 安全（initialState 不依赖 localStorage），后端优先于 localStorage |
| **监控 SDK** | monitor-sdk 批量缓冲 + sendBeacon 卸载兼容设计专业 |
| **nginx 安全头** | CSP/HSTS/X-Frame-Options/X-Content-Type-Options/Referrer-Policy/Permissions-Policy 全套配置 |
| **知识图谱** | 自实现 force simulation 无 d3 依赖，轻量 |
| **Playwright 自动化** | permit_scraper 实现了 CAS 登录+RSA 加密+验证码识别+全模块巡检，技术难度高 |
| **密码安全** | auth_service.py bcrypt rounds=12 + JWT 过期 + 登录限流（5次/5min） |

---

## 六、修复路线图

### 第一阶段：紧急止血（1-2 天）

| 优先级 | 任务 | 预估 |
|--------|------|------|
| P0-1 | 轮换 JWT 密钥 + git filter-repo 清史 + .gitignore 修复 + 强制改密码 | 4h |
| P0-2 | 移除 ECOPILOT_LICENSE_SECRET 环境变量后门 | 1h |
| P0-3 | 清理根 package.json QClaw 残留 | 1h |
| P0-4 | 修复 monitor/page.tsx 认证 Bug | 0.5h |
| P1-7 | 生产环境 CORS 改为白名单 | 1h |
| P1-8 | auth_service.py 默认环境改为 production | 0.5h |
| P1-13 | Docker Compose 按服务隔离数据卷 | 2h |

### 第二阶段：架构加固（1-2 周）

| 优先级 | 任务 | 预估 |
|--------|------|------|
| P1-1 | chat_api.py 按模块拆分 | 3-5 天 |
| P1-5 | permit_scraper 消除硬编码 + 启动定时清理 | 2 天 |
| P1-6 | 全局消除 except: pass，改为日志记录 | 2 天 |
| P1-10 | knowledge_api 原子写 + 并发锁 | 1 天 |
| P1-12 | API 层加重试/超时/降级 | 2 天 |
| P1-9 | tasks.tsx 接后端 | 2 天 |

### 第三阶段：质量提升（2-4 周）

| 优先级 | 任务 | 预估 |
|--------|------|------|
| P1-2 | 5 个千行组件拆分 | 5 天 |
| P1-3 | Hermes 基座补齐 GEPA 进化层 + 记忆层迁移 SQLite | 1 周 |
| P1-4 | 补前端组件测试 + 后端路由测试 | 1 周 |
| P1-11 | ops_monitor 加数据归档 | 1 天 |
| P2 系列 | 按优先级逐步处理 | 持续 |

---

## 七、风险矩阵

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| git 历史中的用户数据被利用 | 高 | 严重（法律+声誉） | 立即清史 + 轮换密钥 + 通知用户改密码 |
| 授权码被伪造绕过 | 高 | 高（商业模式破坏） | 改非对称签名 + 移除后门 |
| chat_api.py 内存泄漏导致崩溃 | 中 | 高（服务中断） | 拆分 + session 过期清理 |
| Playwright chromium 子进程泄露 | 中 | 中（服务器资源耗尽） | 定时清理 + bare except 改日志 |
| 生产环境 CORS * 导致 CSRF | 中 | 高（数据泄露） | 改白名单 |
| 知识库文件写入损坏 | 低 | 中（数据丢失） | 原子写 + 并发锁 |
| tasks.tsx mock 被用户发现 | 高 | 中（信任损失） | 接后端或标注"演示模式" |

---

## 八、总体判断

EcoPilot 不是一个"套壳 ChatGPT"——它有扎实的垂直场景（排污许可全链条）、有前瞻性的 AI 架构理念（Hermes 4 层）、有专业的设计系统（OKLCH + 语义 token）、有高难度的技术实现（Playwright CAS 登录 + RSA 加密 + 验证码识别）。

但它目前处于"快速原型的骨架已搭好，但关键关节还没焊牢"的阶段：

1. **安全层面有 P0 级事故**——用户数据库泄露到 git 历史 + 授权体系后门，这两个问题不处理，产品上线就是定时炸弹
2. **工程层面有结构性债务**——5184 行的上帝文件 + 5 个千行组件 + 大量静默失败，已经到了不处理就会反噬的阶段
3. **AI 基座层面落地不足**——Hermes 4 层理念只实现了 2.5 层，GEPA 进化层完全缺失，Agent 路由靠关键词匹配

**建议**：先花 1-2 天处理 P0（止血），再花 1-2 周做架构加固（P1 核心项），然后逐步提升质量。产品本身值得继续投入——底子在，只是需要焊牢关节。

---

> 本报告基于 2026-07-19 的代码深读，共覆盖后端 12 个 Python 文件（含 chat_api.py 5184 行全文）+ 前端 ~70 个 TypeScript/TSX 文件 + 部署配置 + git 历史。所有问题均附具体文件路径和行号，可逐项验证。
