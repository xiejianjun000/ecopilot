
## Git 仓库推送 (16:50)

- **仓库**: `C:/Users/Administrator/ecopilot` → `git@github.com:xiejianjun000/ecopilot.git`
- **提交**: `76fe6a2` - "refactor: 重构 EcoPilot 应用架构，新增专家系统与项目规范文档" (185 文件)
- **内容**: 重构应用路由为 views/ 结构、新增 experts store、react-bits 动画组件库、项目规范文档、多语言更新、截图资源
- **网络问题解决**: github.com HTTPS 被墙(Empty reply/连接超时)，api.github.com 可通。通过 API 注册 SSH 公钥，配置 SSH 走 ssh.github.com:443 端口绕过封锁，切换 remote 为 SSH 协议推送成功
- **配置变更**: remote 从 HTTPS 改为 SSH；SSH config 添加 github.com → ssh.github.com:443；.gitignore 新增 *.bak/*.bak2 规则
