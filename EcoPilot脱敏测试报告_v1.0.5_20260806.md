# EcoPilot v1.0.5 生产部署前脱敏测试报告

**测试时间**：2026-08-06
**测试对象**：v1.0.5 安装包产物（win-unpacked 全部资源）、desktop 源码、GitHub 公开仓库（main + 提交历史）
**测试方式**：模式扫描 + 人工上下文复核 + 功能验证

---

## 一、扫描结果总览

| # | 扫描项 | 范围 | 结果 |
|---|--------|------|------|
| 1 | 演示企业名/账号（冷水江/yuanbin 等） | 安装包 server/ + app/ | ✅ 零残留 |
| 2 | 硬编码密钥/令牌（sk-/ghp_/AKIA/Bearer） | 安装包 | ❌ **发现 1 处（已修复）** |
| 3 | 手机号/身份证号 | 安装包 | ✅ 零命中 |
| 4 | 邮箱地址 | 安装包 | ✅ 零命中（除占位域名） |
| 5 | 真实 IP 地址 | 安装包 | ⚠️ 3 处（见下文评估） |
| 6 | 演示人名/测试企业（张三/测试账号等） | 源码 | ✅ 零命中 |
| 7 | 硬编码密码 | 源码 | ✅ 零命中（passwd 命中为安全测试用例） |
| 8 | GitHub 公开仓库凭据 | main 分支 | ❌ **发现 1 处（已修复）** |

## 二、发现与处置

### ❌ 发现 1（高危，已修复）：MCP 知识库凭据硬编码并进入公开仓库

- **位置**：`desktop/server/mcp_servers.json`
- **内容**：EHS 知识库服务地址 `http://111.230.89.107:8000/sse/` + Bearer Token
- **暴露面**：① v1.0.4/v1.0.5 安装包内明文；② GitHub **公开仓库** main 分支（2026-08-05 打包提交引入）
- **处置**（提交 `640b198`，已推送并 API 验证）：
  - 仓库内文件改为 `${ECOPILOT_MCP_KB_URL}` / `${ECOPILOT_MCP_KB_TOKEN}` 占位符
  - `mcp_client.py` 新增加载链：`mcp_servers.local.json`（已 gitignore，真实凭据）→ 仓库占位符文件 + 环境变量展开
  - `chat_api.py` 列表接口同步优先读 local 配置（接口本就不回传 headers，无 API 泄露面）
  - 功能验证：local 优先 ✅ / 环境变量展开 ✅ / 无凭据零泄露 ✅

### ⚠️ 待办（需你决策）：轮换知识库 Token

该 Token 在公开仓库暴露了约 1 天（2026-08-05 → 08-06）。git 历史中仍可查到，**建议到 111.230.89.107 服务器上轮换该 Bearer Token**，然后：
- 更新本地 `desktop/server/mcp_servers.local.json`（构建用）
- 下次构建的安装包自动携带新 Token

> 备选方案是改写 git 历史（filter-repo + force push），但会破坏已有克隆，且 Token 已泄露的事实不变——轮换是更彻底的解法。

### ⚠️ IP 地址评估（可接受，无需处理）

| IP | 归属 | 评估 |
|----|------|------|
| 111.230.89.107 | 自有 EcoSkills 注册广场 + 知识库 | 自有资产，代码注释已注明「域名备案中，上线后改 https://ecoskill.cn」 |
| 114.251.10.30 | 全国碳排放报送平台（政府系统） | 公共服务地址，非敏感 |
| 131.0.0.0 | Chrome UA 字符串片段 | 误报 |

## 三、v1.0.5 安装包处置说明

已发布的 v1.0.5 安装包内仍含真实知识库 Token（与 v1.0.4 相同）。这是**共享服务凭据**的设计现状——任何安装包副本都可逆向提取，彻底解法是后续走 API 聚合网关按租户签发。本次不强制重打包；**Token 轮换后顺势出 v1.0.6 即可**。

## 四、结论

- 一轮扫描发现 **1 个真实高危泄露（MCP 凭据入公开仓库）**，已修复并验证
- 其余项全部通过，安装包与源码无演示数据、无 PII、无私钥
- **阻塞项**：知识库 Token 轮换（需要你登录 111.230.89.107 操作）
