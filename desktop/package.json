{
  "name": "ecopilot-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "EcoPilot — 企业生态环境合规AI管家",
  "scripts": {
    "dev": "pnpm --filter ecopilot-desktop dev",
    "test": "pnpm --filter ecopilot-frontend test",
    "test:frontend": "pnpm --filter ecopilot-frontend test",
    "build:frontend": "pnpm --filter ecopilot-frontend build",
    "build:icons": "pnpm --filter ecopilot-desktop build:icons",
    "dist": "pnpm --filter ecopilot-desktop dist"
  }
}
