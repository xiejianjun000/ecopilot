# EcoPilot 发布前安全审计报告

**审计日期**: 2026-08-09  
**审计范围**: `~/dev/ecopilot/` — 前端 :3000 (Next.js) + 后端 :8002 (FastAPI)  
**审计方式**: 被动检查（未修改任何文件或数据）  
**审计结论**: 🔴 **不建议当前状态发布**，存在 1 项阻断级问题需立即修复

---

## 漏洞总览

| 严重程度 | 数量 | 说明 |
|----------|------|------|
| 🔴 CRITICAL | 1 | ECOPILOT_DEV=1 在生产环境中启用 |
| 🟠 HIGH | 3 | 前端零安全头 / npm 28高危 / Python 34漏洞 |
| 🟡 MEDIUM | 4 | CSP 弱化 / 无 HSTS / 内存限流 / Next.js 版本滞后 |
| 🟢 LOW | 3 | 信息泄露头 / 明文占位符 / 静态密钥文件 |

---

## 🔴 CRITICAL — 必须立即修复

### C1. `ECOPILOT_DEV=1` 在生产环境中启用

**影响**: 开发模式标志当前设置为 `1`，导致以下安全机制被绕过：
- SMS 验证码**明文返回**（而非发送短信）
- 密码重置码**明文返回**
- **Dev Bypass Session**（`__dev_bypass__`）可用，允许绕过认证
- 日志提示：`⚠️ ECOPILOT_DEV=1 已启用（SMS 验证码将明文返回），仅限开发环境使用！`

**证据**:
```
~/.ecopilot-home/.env → ECOPILOT_DEV=1
chat_api.py:719 → 仅在 ECOPILOT_DEV=1 时明文返回验证码
chat_api.py:2977 → __dev_bypass__ session 绕过认证
```

**修复**: 从 `~/.ecopilot-home/.env` 中删除或注释 `ECOPILOT_DEV=1`，生产环境绝不可设置此变量。

---

## 🟠 HIGH — 发布前应修复

### H1. Next.js 前端完全缺失安全响应头

**影响**: 前端 :3000 未配置任何安全头，存在点击劫持、MIME 嗅探、XSS 等风险。

**当前状态**:
```
HTTP/1.1 200 OK
X-Powered-By: Next.js        ← 信息泄露
(无 CSP)
(无 X-Frame-Options)
(无 X-Content-Type-Options)
(无 X-XSS-Protection)
(无 Strict-Transport-Security)
(无 Referrer-Policy)
(无 Permissions-Policy)
```

**对比后端**（已正确配置）:
```
X-Content-Type-Options: nosniff          ✓
X-Frame-Options: DENY                    ✓
X-XSS-Protection: 1; mode=block          ✓
Referrer-Policy: strict-origin-when-cross-origin ✓
Permissions-Policy: camera=(), microphone=(), geolocation=() ✓
Content-Security-Policy: ...             ✓
```

**修复**: 在 `next.config.mjs` 中添加 `headers()` 配置，或在 `middleware.ts` 中注入安全头。至少应包含：
```js
async headers() {
  return [{ source: '/(.*)', headers: [
    { key: 'X-Frame-Options', value: 'DENY' },
    { key: 'X-Content-Type-Options', value: 'nosniff' },
    { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
    { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
  ]}]
}
```

### H2. npm 依赖存在 28 个 HIGH + 1 个 CRITICAL 漏洞

`pnpm audit` 结果: **78 vulnerabilities (8 low / 41 moderate / 28 high / 1 critical)**

#### 关键高危漏洞摘要

| 包名 | 高危数 | 影响 |
|------|--------|------|
| **tar** (via electron-builder) | 1 CRITICAL + 7 HIGH | DoS、任意文件覆写、路径遍历、符号链接投毒 |
| **next** (16.2.6 → 需 16.2.11+) | 4 HIGH | SSR 中间件绕过、SSRF in Server Actions、SSRF in rewrites、DoS |
| **electron** | 5 HIGH | Use-after-free、上下文隔离绕过、沙箱绕过、跨域读取 |
| **postcss** | 2 HIGH | 任意文件读取、路径遍历 |
| **electron-updater** | 2 HIGH | 跨域重定向泄露、非受控搜索路径 |
| **semver** | 1 HIGH | ReDoS |
| **ini** | 1 HIGH | 原型污染 |
| **sharp** | 1 HIGH | libvips 继承漏洞 |
| **fast-uri** | 1 HIGH | 主机混淆(backslash) |
| **js-yaml** | 1 HIGH | 二次CPU消耗 |
| **nanoid** | 1 HIGH | 无限循环 |

**修复建议**:
1. **next**: 升级到 `>=16.2.11`（当前 16.2.6）
2. **electron**: 升级到 `>=39.8.10`
3. **electron-builder**: 升级可解决所有 tar 相关漏洞（tar 漏洞仅影响构建阶段，不进入运行时）
4. **postcss**: 升级到 `>=8.5.18`
5. 运行 `pnpm update` 批量更新非破坏性补丁

### H3. Python 环境 34 个已知漏洞（11 个包）

| 包 | 漏洞数 | 关键 CVE |
|----|--------|----------|
| **torch** 2.2.2 | 9 | CVE-2025-2148/2149/2953/2998/2999/3000/3001/3730/32434 |
| **starlette** 1.0.1 | 4 | CVE-2026-48817/48818/54282/54283 |
| **tornado** 6.5.5 | 4 | CVE-2026-49853/49854/49855 (信息泄露 + DoS) |
| **python-multipart** 0.0.27 | 4 | CVE-2026-53537/53538/53539/53540 |
| **pip** 24.0 | 4 | CVE-2026-1703/3219 |
| **onnxruntime** 1.23.2 | 2 | — |
| **pytest/idna/msgpack** | 各1 | CVE-2025-71176 / CVE-2026-45409 |

**注意**: safety 扫描的是全局 Python 环境，非仅 EcoPilot 依赖。实际 EcoPilot `requirements.txt` 声明的核心依赖（fastapi、uvicorn、openai、httpx）较精简。但生产环境中若安装了 torch/onnxruntime，建议同步升级。

**修复建议**:
- `starlette`: 升级到 `>=1.3.1`（影响 FastAPI 底层）
- `python-multipart`: 升级到 `>=0.0.31`
- `tornado`: 升级到 `>=6.5.6`

---

## 🟡 MEDIUM — 建议修复

### M1. 后端 CSP 使用了 `'unsafe-inline'` 和 `'unsafe-eval'`

```python
"script-src 'self' 'unsafe-inline' 'unsafe-eval'; "
"style-src 'self' 'unsafe-inline'; "
```

这大幅削弱了 CSP 防护能力，XSS 攻击者可以注入内联脚本和 eval 执行。建议使用 nonce 或 hash 替代。

### M2. 无 HSTS（Strict-Transport-Security）

后端仅在 HTTPS 下才添加 HSTS 头：
```python
if request.url.scheme == "https":
    response.headers["Strict-Transport-Security"] = "..."
```

当前 localhost 环境无 HTTPS，但若后续部署到公网域名，需确保 HSTS 生效。建议始终添加（localhost 下无害）。

### M3. 速率限制为内存实现，无持久化

- 窗口: 60秒 / 300请求（5 req/s）
- 存储: 进程内存 dict（重启丢失）
- 无分布式支持（多进程/多实例各自独立计数）

当前对单机部署足够，但若扩展需考虑 Redis 方案。

### M4. Next.js 版本滞后于安全补丁

当前 `next@16.2.6`，修复上述4个高危漏洞需要 `>=16.2.11`。滞后 5 个小版本。

---

## 🟢 LOW — 可延后处理

### L1. 信息泄露响应头

- 前端: `X-Powered-By: Next.js`
- 后端: `server: uvicorn`

**修复**: Next.js 可通过 `next.config.mjs` 移除；uvicorn 可通过 `--no-server-header` 或中间件覆盖。

### L2. CLAUDE.md 包含 API Key 占位符（非真实密钥）

```
CLAUDE.md:161: DEEPSEEK_API_KEY=sk-you...-key
CLAUDE.md:163: KIMI_API_KEY=sk-you...-key
```

虽为占位符，但格式与真实密钥一致，可能触发自动化扫描告警。建议改为 `sk-your-key-here` 或删除。

### L3. 静态 JWT Secret 文件

```
ecopilot-website/deploy/data/.jwt_secret (已 gitignored ✓)
.secure-creds/ecopilot_deploy_key (已 gitignored ✓, 权限 600 ✓)
```

文件均在 `.gitignore` 中且权限正确，无泄露风险。但 `.jwt_secret` 为硬编码静态密钥，建议改为环境变量注入。

---

## ✅ 已正确实施的安全措施

| 措施 | 状态 | 说明 |
|------|------|------|
| .env 不在 git 中 | ✅ | `.gitignore` 包含 `.env`/`.env.local`/`.env.*.local` |
| .env 文件权限 600 | ✅ | `~/.ecopilot-home/.env` 仅 owner 可读写 |
| Session token 文件权限 600 | ✅ | `~/.ecopilot-home/.session` 使用 `os.open(…, 0o600)` |
| Auth token 使用 `secrets.token_hex(32)` | ✅ | 64字符随机hex，`secrets.compare_digest` 比对 |
| API 速率限制已实施 | ✅ | 300 req/60s，超限返回 429 |
| 后端安全响应头 | ✅ | CSP/X-Frame-Options/X-Content-Type-Options 等均已配置 |
| 堆栈跟踪未泄露 | ✅ | 错误统一返回 JSON `{"detail": "..."}` |
| 错误响应包含 CORS 头 | ✅ | 中间件返回 401/403/429 时手动注入 CORS 头 |
| API 认证中间件 | ✅ | Bearer token + 许可证双重验证 |
| .secure-creds 已 gitignored | ✅ | `git check-ignore` 确认 |
| CORS 限制为 localhost | ✅ | 仅允许 `127.0.0.1:3000` 和 `localhost:3000` |

---

## 修复优先级建议

```
P0 (阻断): 关闭 ECOPILOT_DEV=1
P1 (本周): Next.js 升级到 16.2.11+、添加前端安全响应头
P2 (本周): 升级 starlette/python-multipart/tornado
P3 (下周): 修复 CSP unsafe-inline、移除信息泄露头
P4 (下版): 升级 electron/electron-builder、HSTS增强
```

---

> *本报告由 Hermes Agent 自动审计生成，仅做被动检查，未修改任何项目文件。*
