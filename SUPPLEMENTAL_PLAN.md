# EcoPilot 补充开发方案 v2.1

> 基于现有代码（2026-06-26 盘点）的补充开发计划 + SaaS 订阅与智能体设计
> 2026-06-26

---

## 第一部分：现有代码状态盘点

### 1.1 已完成模块（可直接使用）

| 模块 | 文件 | 行数 | 说明 |
|------|------|------|------|
| **引导流程 8 步** | `onboarding/` 下 8 个文件 | 共 908 行 | 品牌动画 → 许可证上传 → OCR确认 → 平台登录 → 自动巡检 → 报告 → 档案 → 注册 |
| **引导状态机** | `store/onboarding.ts` | 197 行 | 完整状态 + action + 档案清单生成 |
| **许可证合规状态** | `store/permit.ts` | 169 行 | 合规状态 + 排放告警 + 演示数据（冷钢） |
| **巡检定时任务** | `store/patrol.ts` | 129 行 | 4 个预设任务 + toggle/run |
| **许可证 OCR 解析** | `lib/permit-parser.ts` | 162 行 | 结构化类型 + 正则解析 + 到期计算 |
| **政务平台 URL** | `lib/platform-urls.ts` | 131 行 | 7 个核心平台 + 排污许可登录 RPA 细节 |
| **Obsidian 同步** | `lib/vault-sync.ts` | 274 行 | 企业主页/许可证/排放口模板 + YAML 生成 |
| **仪表盘 UI** | `dashboard/index.tsx` | 336 行 | 待处理事项、排放概览、知识统计、快捷操作 |
| **许可证组件** | `components/permit-card.tsx` | 76 行 | 许可证卡片 + 倒计时 |
| **合规徽章** | `components/compliance-badge.tsx` | 34 行 | safe/warning/urgent/expired |
| **Hermes 客户端** | `hermes-client.ts` | 259 行 | WebSocket JSON-RPC + REST + 流事件 |
| **全局样式** | `styles.css` | 867 行 | 双主题 + 所有页面组件样式 |
| **EcoPilotShell 页面壳** | `EcoPilotShell.tsx` | 1476 行 | 导航/会话列表/9页面/右侧面板完整 UI |

### 1.2 基本完成（缺真实后端对接）

| 模块 | 问题 |
|------|------|
| **EcoPilotShell.tsx** | 所有数据用 `loadDemoCompliance()` 模拟，无真实后端 |
| **dashboard/index.tsx** | UI 完整但演示数据 |
| **EcoPilot 入口 index.tsx** | 引导判断逻辑双分支都返回仪表盘，**引导流程未接入** |

### 1.3 骨架/占位（需从零开发）

| 模块 | 文件 | 说明 |
|------|------|------|
| **专家面板** | `app/ecopilot/experts/index.tsx` | 15 行 "专家功能开发中..." |
| **合规日历** | `app/ecopilot/calendar/index.tsx` | 15 行 "日历功能开发中..." |
| **档案库** | `app/ecopilot/doc-vault/index.tsx` | 15 行 "档案库功能开发中..." |
| **平台账号管理** | `app/ecopilot/settings/platform-accounts.tsx` | 15 行 "平台账号管理功能开发中..." |

### 1.4 关键问题汇总

1. **引导流程未接入入口** — app/ecopilot/index.tsx 的判断逻辑没有真正区分已引导/未引导
2. **4 个核心模块是空壳** — 专家、日历、档案库、平台账号管理
3. **无真实后端对接** — 全部使用演示数据
4. **对话已接上 Hermes Dashboard** — 但 Hermes 的 SOUL.md 还没有完全替换为 EcoPilot 身份
5. **语音、附件功能未实现**（输入栏有按钮但无逻辑）
6. **SaaS 订阅系统未实现**
7. **专家创建/雇佣体系未实现**
8. **右侧面板（记忆/日记/资产）是静态占位**

---

## 第二部分：补充开发计划（分阶段）

### P0 - 串联核心流程（3-5 天）

```
目标：用户从下载安装到完成引导进入仪表盘的完整闭环
```

| # | 任务 | 工作量 | 涉及文件 |
|---|------|--------|---------|
| 0.1 | **修复引导入口** — `app/ecopilot/index.tsx` 中 `$onboarding.completed` 判断后真正显示 `OnboardingPage` 或 `DashboardPage` | 小 | `index.tsx` |
| 0.2 | **引导 → 仪表盘数据传递** — 引导完成后把许可证信息写入 `$compliance`，替换演示数据 | 中 | `onboarding/register.tsx` → `store/permit.ts` |
| 0.3 | **Hermes SOUL.md 永久替换** — 确保每次启动用 EcoPilot 身份而非 Hermes | 小 | `SOUL.md` |
| 0.4 | **对话首条消息优化** — 系统提示词精简，不让 AI 主动发自我介绍 | 小 | `EcoPilotShell.tsx` → `ECOPROMPT` |

### P1 - 4 个核心模块补齐（5-7 天）

```
目标：专家面板、合规日历、档案库、平台账号管理从占位变成可用
```

#### 1.1 专家面板（EcoPilot 子路由）

参考 `EcoPilotShell.tsx` 中已有的 ExpertPanel（411-438 行）设计，实现独立路由页面。

**现有参考：**
```typescript
const EXPERTS = [
  { id: 'ecomind', name: '综合管家', desc: '全链条统筹协调', ... },
  { id: 'permit', name: '排污许可专家', desc: '许可证申领/变更/延续', ... },
  { id: 'carbon', name: '碳排放专家', desc: '碳核算/配额/碳市场', ... },
  { id: 'env-monitoring', name: '环境监测专家', desc: 'CEMS/自行监测/数据解读', ... },
  { id: 'compliance', name: '合规巡检专家', desc: '台账管理/自查自纠', ... },
  { id: 'emergency', name: '应急专家', desc: '应急预案/隐患排查', ... },
  { id: 'cleaner', name: '清洁生产专家', desc: '清洁生产/绿色工厂', ... },
]
```

**需要实现：**
- [ ] 专家卡片列表（从 store 读取，非硬编码）
- [ ] "我的专家" / "浏览专家库" tab 切换
- [ ] 专家详情/编辑弹窗
- [ ] 浮窗专家栏（对话底部快速注入专家）
- [ ] **专家圆桌会议**（EcoPilotShell 已有完整 UI 可复用）

#### 1.2 合规日历

- [ ] 月历视图 + 事件标记（许可证到期、报告截止等）
- [ ] 事件从 `$compliance` + `$patrolJobs` 读取
- [ ] 点击事件跳转到详情

#### 1.3 档案库

- [ ] 复用 `doc-collection.tsx` 的分类列表设计
- [ ] 文件上传（拖拽/点击）
- [ ] 按类别/状态筛选
- [ ] 档案完整性仪表盘

#### 1.4 平台账号管理

- [ ] 从 `platform-urls.ts` 读取 7 个核心平台
- [ ] 每个平台的账号/密码/UKey 凭据管理
- [ ] 凭据本地加密存储（非明文）

### P2 - SaaS 订阅 + 智能体体系（5-7 天）

```
目标：用户能付费升级、能创建和管理自定义专家智能体
```

#### 2.1 订阅系统

| 套餐 | 价格 | 月对话次数 | 专家席位 | 自定义智能体 | 政务平台 | 记忆/日记/资产 |
|------|:----:|:----------:|:--------:|:-----------:|:--------:|:-------------:|
| **免费版** | ¥0 | 50次/月 | 1（仅助手） | 0 | 仅内置浏览器 | ❌ |
| **基础版** | ¥99/月 | 500次 | 3（含助手） | 1 | 7 个可填写 | ✅ 基础版 |
| **专业版** | ¥299/月 | 2000次 | 7（全部） | 3 | 20 个全部 | ✅ 完整版 |
| **企业版** | ¥999/月 | 不限 | 7+自定义 | 10 | 全部+API | ✅ 完整版+导出 |

#### 2.2 智能体（自定义专家）创建系统

设计见第四部分。

### P3 - 持续打磨（穿插进行）

- [ ] 输入框语音/附件功能
- [ ] 右侧面板（记忆/日记/资产）从静态占位变为真实内容展示
- [ ] 加载骨架屏 / 空状态组件
- [ ] 系统托盘 + 通知
- [ ] 快捷键系统

---

## 第三部分：现有代码架构的改进建议

### 3.1 架构问题

1. **EcoPilotShell.tsx 过于庞大**（1476 行）— 内嵌了太多页面组件，建议将各页面逐步抽离到独立路由
2. **EcoPilot 子路由未真正接入** — `routes.ts` 定义了 `/dashboard`, `/experts` 等路由但入口未用 `react-router`
3. **两套布局并存** — `EcoPilotShell.tsx` 和 `MainLayout.tsx` 功能重叠

### 3.2 推荐架构演进

```
阶段一：EcoPilotShell 为主入口，保持现有结构，逐步替换占位页面
阶段二：引入 react-router（已有 routes.ts），将页面剥离为独立路由
阶段三：移除重复布局代码，统一使用一套布局
```

---

## 第四部分：SaaS 订阅与智能体体系详细设计

### 4.1 订阅层级设计原则

1. **免费版是漏斗入口** — 限制用量但不限制核心功能体验
2. **基础版是转化门槛** — 每月 500 次对话够中小企业日常使用
3. **专业版是主力收入** — 完整专家阵容 + 政务平台对接
4. **企业版是定制化** — 不限量 + 自定义智能体 + API

### 4.2 智能体（自定义专家）是什么

**智能体 = 用户自定义的专家插件。**

不是独立的对话窗口，而是注入到助手（Hermes）system prompt 中的专业技能包。用户创建智能体时定义：

```
智能体 = 名称 + 角色描述 + 系统提示词 + 可选插件模型 + 可选 MCP 连接器
```

**工作机制（跟 WorkBuddy 一致）：**

```
用户创建智能体（对话引导 / 3步弹窗）
  → 生成 manifest JSON
  → 用户对话时点击智能体卡片
  → 智能体配置注入当前会话的 system prompt
  → 助手在相关问题时自动调用该智能体的能力
```

### 4.3 智能体创建流程（双路径）

**路径一：对话创建**
```
用户点击 [+ 创建智能体]
  → 导航回对话页
  → 输入框自动填入特殊指令
  → 用户发送 → AI 对话引导用户完成创建
  → AI 根据对话内容自动生成 system prompt + 能力描述
  → 生成 manifest JSON + 保存
```

**路径二：3 步弹窗**

```
第一步：基础信息
  智能体名称: [文本输入]
  角色快捷选择: [碳排放] [排污许可] [法规] [自定义...]
  角色描述: [文本区域]

第二步：配置
  选择模型: [DeepSeek-Chat ▼]
  雇佣专家插件: [从已安装专家的选一个]
  或上传专家包: [拖拽 .tar.gz]

第三步：确认
  摘要: 名称 + 描述 + 模型 + 插件
  [创建]
```

### 4.4 智能体 Manifest 结构

```json
{
  "id": "custom-carbon-analyst",
  "name": "碳排放分析师",
  "description": "负责企业碳排放核算与碳资产管理",
  "avatar": "avatar-3",
  "model": "deepseek-chat",
  "createdAt": "2026-06-26T10:00:00Z",
  "useCount": 0,
  "manifest": {
    "manifestVersion": "1.0",
    "system_prompt": "你是碳排放分析师，擅长...",
    "annotations": {
      "agent-title": "碳排放管理"
    },
    "plugins": [],
    "mcp": [],
    "memory": { "enable": true }
  }
}
```

### 4.5 智能体与订阅层级的映射

| 能力 | 免费版 | 基础版 | 专业版 | 企业版 |
|------|:------:|:------:|:------:|:------:|
| 预置专家 | 仅助手 | 助手+2 | 7 位全部 | 7 位全部 |
| 自定义智能体 | 0 | 1 | 3 | 10 |
| 智能体创建方式 | - | 弹窗 | 弹窗+对话 | 弹窗+对话 |
| 雇佣专家插件 | ❌ | ❌ | ✅ 市场安装 | ✅ 市场+自定义 |
| 智能体分享 | ❌ | ❌ | ❌ | ✅ 团队共享 |
| 智能体 API | ❌ | ❌ | ❌ | ✅ REST API |

### 4.6 智能体席位管理

```
导航栏底部 / 设置中的用量面板:
┌──────────────────────────────┐
│  当前套餐: 专业版            │
│  对话次数: 2000次/月         │
│  [██████░░░░] 已用 1200次   │
│                              │
│  智能体席位: 3/3             │
│  [████████████████████] 已满 │
│                              │
│  [升级企业版] [管理订阅]      │
└──────────────────────────────┘
```

超额处理策略：
- 对话次数超限 → 降速（非高峰时段回复），提示升级
- 智能体超限 → 提示删除已有或升级，可停用但不删除

### 4.7 智能体市场（付费生态）

```
智能体市场（专业版+可用）:
├── 分类: 碳排放 / 排污许可 / 监测 / 法规 / 固废 / 应急
├── 每个智能体: 封面 + 评分(1-5⭐) + 安装量 + 价格
├── 免费 / 付费（开发者分成）
└── 一键安装 → 出现在"我的智能体"列表
```

### 4.8 用量计量设计

```
API 网关层:
  每次对话请求 → 计量中间件 → 查询用户套餐 → 检查余量
    → 有余量: 扣减 + 放行
    → 无余量: 返回 402 Payment Required + 提示升级

对话次数定义:
  - 用户发送一条消息 = 1 次对话
  - 同 session 连续对话连续计费
  - 工具调用不计额外费用

存储计量:
  - 记忆条数: 免费 100 条 / 专业版 10000 条
  - 资产文件: 免费 50MB / 专业版 10GB
```

---

## 第五部分：实施优先级建议

| 优先级 | 模块 | 预计工时 | 依赖 |
|--------|------|---------|------|
| **P0-1** | 入口引导修复 | 0.5天 | 无 |
| **P0-2** | 引导 → 仪表盘数据传递 | 1天 | P0-1 |
| **P0-3** | 4 个占位页面补齐 | 5-7天 | 无 |
| **P1-1** | 对话深度完善（语音/附件/流式渲染） | 2天 | 无 |
| **P1-2** | 右侧面板（记忆/日记/资产）真实化 | 3天 | P0-3 |
| **P2-1** | 订阅付费系统 UI + 计量 | 3天 | 无 |
| **P2-2** | 智能体创建系统（弹窗路径） | 4天 | P1-1 |
| **P2-3** | 智能体注入对话（浮窗专家栏） | 2天 | P2-2 |
| **P3-1** | 专家圆桌会议 | 3天 | P2-3 |
| **P3-2** | 智能体市场 | 5天 | P2-2 |
| **P4** | 企业版功能（多用户/API/审计） | 5天 | P2 |

---

## 第六部分：现有可立即对接的资源

### 6.1 可直接复用的代码

| 资源 | 来源 | 用途 |
|------|------|------|
| ExpertPanel + EXPERTS 常量 | `EcoPilotShell.tsx:411-438` | 专家卡片列表 UI |
| 圆桌会议 | `EcoPilotShell.tsx:1167-1415` | 多专家并行讨论 |
| MeetingSetup/Summoning | `EcoPilotShell.tsx` | 会议动画 + 议题 |
| SelfLearningPanel | `EcoPilotShell.tsx` | 自学习展示 |
| patrol.ts 4 个任务 | `store/patrol.ts` | 合规巡检定时任务 |
| platform-urls.ts 7 平台 | `lib/platform-urls.ts` | 政务平台管理 |
| vault-sync.ts | `lib/vault-sync.ts` | 知识库同步 |
| hermes-client.ts | `hermes-client.ts` | 后端通信 |

### 6.2 建议立即启动的工作

1. **修复引导入口** — 半小时的工作但影响整个用户流
2. **替换 Hermes SOUL.md** — 确保 AI 以 EcoPilot 身份回复
3. **专家面板开发** — EcoPilotShell 已有完整专家 UI 参考，只需抽离为独立路由

---

## 附录：关键决策记录

| 问题 | 决策 | 理由 |
|------|------|------|
| 智能体用 Hermes 还是独立模型？ | 所有智能体共用 Hermes 底座，通过 system prompt 差异化 | WorkBuddy 已验证模式 |
| 对话次数如何定义？ | 用户每次发送 = 1 次 | 简单清晰，对标主流 |
| 免费版是否限制功能？ | 不限制功能但限制数量 | 让用户体验完整价值 |
| 专家和智能体的关系？ | 预置专家 = 官方智能体，自定义智能体 = 用户创建的 | 统一为"智能体"概念 |
